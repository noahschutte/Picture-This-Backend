<%= link_to "Back", user_rounds_path(@user) %>
<div class="deck-list-header">Choose a Deck:</div>
<ul class="deck-list">
  <% @decks.each do |deck| %>
  <li>
    <%= link_to "#{deck.name}", prompt_contacts_path(@prompt)%>
  </li>

  <% end %>
</ul>
